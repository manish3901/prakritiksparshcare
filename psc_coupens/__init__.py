"""
Namespace package for the external coupon system.

We keep the coupon engine code under `psc_coupens/psc_coupens_app` so it can be
mounted inside the main PSC portal at `/external-coupen-system`.
"""

